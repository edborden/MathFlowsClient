if invalid
	invalidation-popover block=block

.grid-stack-item-content
	if block
		if block.question
			.question-number
				block.questionNumber
				|)
		each block.sortedLines as |line| 
			equation-renderer line=line preview=preview
		if block.image
			block-image image=block.image preview=preview
		if block.table
			block-table table=block.table preview=preview